<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vinayak.persondetailsdatabase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonDetailsDatabase">
        <activity
            android:name=".Show_Details"
            android:exported="true" />
        <activity
            android:name=".Flash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Delete_person" />
        <activity android:name=".View_person" />
        <activity android:name=".Add_person" />
        <activity android:name=".Home" />
        <activity android:name=".All_mobile" />
        <activity android:name=".Person_details" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>